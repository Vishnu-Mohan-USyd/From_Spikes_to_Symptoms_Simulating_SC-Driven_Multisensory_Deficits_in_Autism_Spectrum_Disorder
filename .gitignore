# python
__pycache__/
*.pyc

# runtime scratch — all regenerated by a measurement run; NOT part of the committed deliverable
out/                              # routec_net_io OUT_DIR (per-run intermediate)
results/*_g[0-9].json             # per-GPU-lane partial JSONs (gates_main_g0.json, gate5_cre_g1.json, ...)
results/parallel_lane_gpu*.out    # per-lane stdout logs
results/parallel_summary.json     # lane-split bookkeeping
results/*.png                     # cue_reliability_routec quick-look figs; house-style deliverables live in figures/
results/*.svg

# the COMMITTED deliverable keeps: all *.py, README.md, CKPT_MD5.txt, checkpoint/*.pt (10 models),
# fonts/, figures/ (20 PNG+SVG), results/ canonical JSONs (gates_main/gate5_cre/gate6_latency_sweep +
# gate7_cuerel_g{1,2}_seed*.json + *_aggregate.json), records/.
